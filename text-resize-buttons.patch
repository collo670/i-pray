diff --git a/js/text-size-control.js b/js/text-size-control.js
index 0706e98..fead491 100644
--- a/js/text-size-control.js
+++ b/js/text-size-control.js
@@ -1,9 +1,12 @@
 // ---------------------------------------------------------------------------
 // Shared text-size control (A− / A / A+) for prayer pages that don't load
 // js/masifu.js: midday (saa-sita), vespers (jioni) and office of readings
-// (ofisi ya masomo). Scales the root <html> font-size so every rem/em-based
-// rule on the page — psalm text included — grows and shrinks together, and
-// persists the choice across all prayer pages via localStorage.
+// (ofisi ya masomo) — including the English versions of all three, pulled
+// live from Universalis in pages/prayer-hour.html. Sets --prayer-text-scale
+// on the root <html> element; each page's own CSS decides which container
+// (.office-content, .reading-text, etc.) multiplies its font-size by that
+// variable, and the chosen scale persists across all prayer pages via
+// localStorage.
 // ---------------------------------------------------------------------------
 
 (function () {
diff --git a/pages/prayer-hour.html b/pages/prayer-hour.html
index 404f89d..0bba455 100644
--- a/pages/prayer-hour.html
+++ b/pages/prayer-hour.html
@@ -18,7 +18,11 @@
     <style>
         /* Universalis office markup (.v verse lines, .vi continuations, table.each
            section headings, rubrics…) restyled to flow with the app layout. */
-        .office-content { font-size: 1.0625rem; line-height: 1.75; color: #374151; }
+        /* --prayer-text-scale is set by js/text-size-control.js (default 1)
+           and applies to the whole rendered office - English (Universalis)
+           and Swahili alike - across all three hours (Readings, Midday,
+           Vespers), since every view renders into this one container. */
+        .office-content { font-size: calc(1.0625rem * var(--prayer-text-scale, 1)); line-height: 1.75; color: #374151; }
         .dark .office-content { color: #d1d5db; }
         .office-content .v { margin: 0; }
         .office-content .gb { margin-top: 0.9em; }
@@ -63,11 +67,6 @@
         .dark .office-content .sw-speaker { color: #c4b5fd; }
         .office-content .sw-fallback-note { font-size: 0.85rem; font-style: italic; color: #b91c1c; background: rgba(185,28,28,0.08); border-radius: 0.5rem; padding: 0.6rem 0.9rem; margin: 0 0 1rem; }
         .dark .office-content .sw-fallback-note { color: #f87171; background: rgba(248,113,113,0.12); }
-        /* --prayer-text-scale is set by js/text-size-control.js (default 1).
-           Scoped to .sw-office only, so the resize control (shown only for
-           the Kiswahili reading view) grows/shrinks that reading text alone -
-           not the English/Universalis view and not the app chrome. */
-        .sw-office { font-size: calc(1rem * var(--prayer-text-scale, 1)); }
     </style>
     <!-- Apply saved theme before paint to avoid flash -->
     <script>
@@ -357,9 +356,13 @@
             }
 
             // js/text-size-control.js (loaded deferred, so it may not have
-            // created its floating button yet on the very first call) only
-            // resizes .sw-office text, so it's only shown for that view -
-            // the Swahili Office of Readings - not English or Midday/Vespers.
+            // created its floating button yet on the very first call) resizes
+            // .office-content text, which every rendered office - English or
+            // Swahili, Readings, Midday or Vespers - shares. It's only hidden
+            // when the rendered office itself isn't on screen (the loading
+            // spinner, the "not available yet" message, or the iframe
+            // fallback for when every Universalis proxy failed), since in
+            // those cases there's no office text on the page to resize.
             function updateTextCtlVisibility() {
                 var ctl = document.querySelector('.prayer-textctl');
                 if (!ctl) return;
@@ -368,7 +371,7 @@
                 // ".prayer-textctl{display:flex}" after Tailwind's stylesheet,
                 // which would otherwise win a same-specificity, later-in-source
                 // tie against a Tailwind "hidden" utility class.
-                ctl.style.display = (hourKey === 'readings' && lang === 'sw') ? '' : 'none';
+                ctl.style.display = !officeEl.classList.contains('hidden') ? '' : 'none';
             }
 
             function applyLangSwitchUI() {
@@ -445,6 +448,7 @@
                 loadingEl.classList.add('hidden');
                 frame.classList.add('hidden');
                 officeEl.classList.remove('hidden');
+                updateTextCtlVisibility();
             }
 
             function fetchThroughProxies(url, token, onOk, onFail) {
@@ -635,6 +639,7 @@
                     windowNote.textContent = (lang === 'sw' ? 'Sala zinapatikana kuanzia ' : 'The office is available from ')
                         + fmt(minDate) + (lang === 'sw' ? ' hadi ' : ' to ') + fmt(maxDate) + '.';
                     unavailableEl.classList.remove('hidden');
+                    updateTextCtlVisibility();
                     return;
                 }
 
@@ -660,6 +665,7 @@
                 if (!frame.src || fallbackToken !== loadToken) return;
                 loadingEl.classList.add('hidden');
                 frame.classList.remove('hidden');
+                updateTextCtlVisibility();
             });
 
             function switchHour(key) {
@@ -712,8 +718,11 @@
         })();
     </script>
     <script src="../js/prayers-nav.js" defer></script>
-    <!-- A-/A+ control that resizes only the rendered Swahili reading text
-         (.sw-office); hidden outside that view by updateTextCtlVisibility(). -->
+    <!-- A-/A+ control that resizes the rendered office text (.office-content)
+         for every hour and language - Readings, Midday and Vespers, English
+         and Swahili alike; hidden only while the office itself isn't on
+         screen (loading, unavailable, or iframe fallback) by
+         updateTextCtlVisibility(). -->
     <script src="../js/text-size-control.js" defer></script>
     <script src="../js/prevent-zoom.js" defer></script>
 </body>
